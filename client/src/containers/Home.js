import React, {useEffect} from 'react';
import propTypes from 'prop-types';
import config from '../config';
const Home = (props)=>{


    return (
        <div>Hello</div>
    )
}

Home.propTypes={

}

export default (Home);